# Copia este archivo a .env para desarrollo local (nunca subas .env real a GitHub)

# Puerto local (Railway define PORT automaticamente en produccion)
PORT=3000

# Contrasena del panel de administracion (/admin). Cambiala por una fuerte.
ADMIN_PASSWORD=cambia-esta-clave

# Alternativa mas segura: un hash bcrypt de la contrasena en vez de texto plano.
# Genera uno con: node -e "console.log(require('bcryptjs').hashSync('tu-clave', 10))"
# Si defines ADMIN_PASSWORD_HASH, tiene prioridad sobre ADMIN_PASSWORD.
# ADMIN_PASSWORD_HASH=

# Clave para firmar la cookie de sesion del admin. Usa un texto largo y aleatorio.
SESSION_SECRET=cambia-esta-clave-tambien

# Base de datos (obligatoria). En Railway, agrega el plugin de PostgreSQL
# (New > Database > PostgreSQL) y esta variable se crea sola.
# En local, usa una URL de un Postgres que tengas corriendo, por ejemplo:
# postgres://postgres:tu-clave@localhost:5432/endulcora
DATABASE_URL=

# Carpeta donde se guardan los archivos subidos (fotos, PDFs). En Railway,
# apunta esto a un Volume, por ejemplo: /data
DATA_DIR=./data

# URL publica del sitio (sin / al final). Se usa para construir los enlaces
# de regreso y el webhook de Mercado Pago.
SITE_URL=https://www.endulcora.com

# Credenciales de Mercado Pago (Developers > Tu app > Credenciales).
# Usa las de "Prueba" mientras pruebas, y cambia a las "Productivas" para cobrar de verdad.
MP_ACCESS_TOKEN=
MP_PUBLIC_KEY=
# Ponla en "true" SOLO mientras MP_ACCESS_TOKEN/MP_PUBLIC_KEY sean las de
# "Prueba", para que el checkout te mande al ambiente de pruebas de Mercado
# Pago en vez del real. Bórrala o ponla en "false" para cobrar de verdad.
MP_SANDBOX=false

# Envio de correos de confirmacion de compra (resend.com). Sin esta variable,
# los pedidos se siguen aprobando normal, pero el correo automatico no se envia
# (tienes que entregar manualmente desde /admin > Ventas, como antes).
RESEND_API_KEY=
# Remitente que ven tus clientes. Mientras no verifiques tu dominio en Resend,
# usa el valor de prueba: "Endulcora <onboarding@resend.dev>"
RESEND_FROM=Endulcora <onboarding@resend.dev>

# Asistente de IA (burbuja de chat del sitio). Consigue una clave gratis en
# aistudio.google.com (Get API key) - no requiere tarjeta para el nivel
# gratuito. Sin esta variable, la burbuja del asistente sigue apareciendo
# pero responde que todavia no esta activado.
GEMINI_API_KEY=

# Meta Pixel / API de Conversiones (mide las visitas y compras para anuncios
# de Facebook/Instagram). El pixel del navegador ya funciona sin estas
# variables; solo hacen falta para mandar el evento Purchase tambien desde
# el servidor (respaldo por si el navegador del comprador bloquea el pixel).
# Se consiguen en Meta Events Manager > tu pixel > Configuracion > API de
# conversiones > Generar token de acceso.
META_PIXEL_ID=1302735011542499
META_CAPI_TOKEN=
# Solo mientras pruebas: pegale el "codigo de prueba" que te da Meta en
# Events Manager > Probar eventos, para ver el Purchase del servidor ahi sin
# ensuciar los datos reales. Bórrala cuando termines de probar.
META_CAPI_TEST_CODE=

# ---------------------------------------------------------------------------
# Google Ads (Endulcora, CRENEF, LEVENT e Instituto Justo)
# ---------------------------------------------------------------------------
# Cada negocio tiene su propia cuenta, pero todas cuelgan de una misma cuenta
# administradora (MCC), y estas credenciales son las del MCC: sirven para las
# cuatro. Paso a paso completo en docs/google-ads.md.

# Cliente OAuth de console.cloud.google.com. Tipo "Aplicacion web", con esta
# URI de redireccionamiento autorizada:
#   https://www.endulcora.com/api/google-ads/oauth/callback
GOOGLE_ADS_CLIENT_ID=
GOOGLE_ADS_CLIENT_SECRET=
# Opcional: el permiso permanente. Si se deja vacia, se consigue con un clic
# desde /api/google-ads/oauth/inicio y el servidor lo guarda en la base de
# datos. Si se pone aqui, esta variable manda sobre lo guardado.
GOOGLE_ADS_REFRESH_TOKEN=
# Centro de API dentro del MCC. Pide "acceso basico": el de prueba no puede
# tocar cuentas reales.
GOOGLE_ADS_DEVELOPER_TOKEN=
# ID de la cuenta administradora (MCC), 10 digitos sin guiones.
# La nuestra es 894-945-9356.
GOOGLE_ADS_MANAGER_ID=8949459356
# Version de la API de Google Ads. Solo se cambia si Google retira la actual.
# GOOGLE_ADS_API_VERSION=v21

# Token propio (invéntalo tú, largo y aleatorio) para operar Google Ads desde
# fuera del servidor, en /api/google-ads. Sin el, ese panel queda apagado.
# Genera uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
GOOGLE_ADS_ADMIN_TOKEN=

# ID de la cuenta de Google Ads de cada negocio (10 digitos, sin guiones). Los
# imprime "node scripts/google-ads.js crear-cuenta <negocio>".
GOOGLE_ADS_CUSTOMER_ENDULCORA=
GOOGLE_ADS_CUSTOMER_CRENEF=
GOOGLE_ADS_CUSTOMER_LEVENT=
GOOGLE_ADS_CUSTOMER_INSTITUTO_JUSTO=

# Sitio de cada negocio (solo para tenerlos juntos; el de Endulcora ya viene
# puesto). Sirven al armar campanas.
# GOOGLE_ADS_SITIO_CRENEF=
# GOOGLE_ADS_SITIO_LEVENT=
# GOOGLE_ADS_SITIO_INSTITUTO_JUSTO=

# Medicion del sitio de Endulcora. Sin GOOGLE_ADS_ID, el sitio no carga nada de
# Google (todo lo demas sigue funcionando igual).
GOOGLE_ADS_ID=
# Etiqueta completa de la conversion de compra: AW-XXXXXXXXXX/AbC-D_efGhIjKl
GOOGLE_ADS_CONVERSION_COMPRA=
# La misma conversion, en ID numerico, para mandarla tambien desde el servidor.
GOOGLE_ADS_CONVERSION_COMPRA_ID=

NODE_ENV=development
